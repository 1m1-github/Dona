# using Metal

# "Used in TOG as the common type. Types should not be mixed, so do use T for constants like T(0.01)."
const T = typeof(1.0)
# const T = Float32
# "Used to observe TOG."
# const GPU_BACKEND = MetalBackend()
# "Used to observe TOG."
# const GPU_BACKEND_WORKGROUPSIZE = 2^2^3

include("/Users/1m1/Documents/TheoryOfGod/src/∃.jl")
